plugins {
    id 'groovy-gradle-plugin'
    id 'maven-publish'
}

def props = new Properties()
file("../gradle.properties").withInputStream {
    props.load(it)
}

group = 'com.sunny.framework.gradle'
version = props['sunnyFrameworkVersion']